Integration diagnosis: the initial `npm run test:integration:local` failure was only Docker-socket permission denial in the current shell. Running it through `sg docker -c 'npm run test:integration:local'` passed the full disposable PostgreSQL lifecycle, including all 5 profile-first optimiser apply tests. The test container was removed; branch is clean. PR #390 remains an open draft, but GitHub marks it BEHIND main and its sole comment says a previously claimed remediation was not recovered. No code defect was reproduced; determine whether upstream changes require a rebase/reimplementation before updating the PR handoff.